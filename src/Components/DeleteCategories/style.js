import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles((theme) => ({
    img: {
        margin: 'auto',
        width: '100%',
    },
    accordion: {
        margin: 'auto !important',
        width: '450px',
    },
    containerGrid: {
        textAlign:'center',
        marginTop:'10px',
    },
}));